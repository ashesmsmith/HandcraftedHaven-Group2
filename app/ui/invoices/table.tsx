/**
 * Invoices Table Component
 * 
 * Purpose:
 * - Displays a paginated table of invoices.
 * - Allows filtering and searching through invoices.
 * - Includes columns for invoice details such as number, date, amount, and status.
 * 
 * Usage:
 * - Used on the `/dashboard/invoices` page.
 * 
 * Props:
 * - `query`: Search query for filtering invoices.
 * - `currentPage`: Current page number for pagination.
 * - Callback for handling actions like sorting or status changes.
 */
