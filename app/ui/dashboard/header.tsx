/**
 * Header Component
 * 
 * Purpose:
 * - Displays a top navigation bar for the dashboard.
 * - Often includes a logo, search bar, notifications, and user profile options.
 * 
 * Usage:
 * - Used in the dashboard layout as a global header for all dashboard pages.
 * 
 * Props:
 * - User information (e.g., name, avatar) for display.
 * - Callback functions for profile menu actions (logout, settings).
 */
